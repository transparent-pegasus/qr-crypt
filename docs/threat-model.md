# Qrypt Threat Model

## 1. Protected assets

| Asset                                              | Storage location                                                          | Sensitivity                                                    |
| -------------------------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------- |
| Plaintext / decrypted message body                 | React memory only (persistence forbidden)                                 | Highest                                                        |
| AES symmetric key                                  | IndexedDB (CryptoKey, extractable — required by the QR exchange spec)     | Highest                                                        |
| RSA private key                                    | IndexedDB (CryptoKey, **non-extractable**)                                | Highest                                                        |
| Symmetric-key QR / private-key backup QR (future)  | No in-app persistence (on-screen display + explicit user export only)     | Highest                                                        |
| Ciphertext QR                                      | Not persisted (session memory + explicit user export only)                | Confidential (cannot be decrypted without the key)             |
| Public-key QR                                      | Same as above                                                             | Public                                                         |
| Settings (scheme, EC level, etc.)                  | IndexedDB / theme `oc-theme` in localStorage                              | Low                                                            |
| Offline acknowledgement pending state              | localStorage `oc-offline-ack-pending="1"`                                 | Low (non-sensitive; contains no keys, plaintext, or ciphertext) |

## 2. Scope of guarantees

What this app guarantees extends only to: **the application does not intentionally transmit plaintext, private keys, or symmetric keys off the device**. Implementation-level supports:

- Zero external API communication as an app feature (no third-party or cross-origin clients). The only permitted network traffic is fetching the PWA itself and its static assets at initial installation, plus the same-origin `GET /reachability-sentinel.txt` probe that gates wipe-on-online (see T18); the probe carries no user data
- CSP `connect-src 'self'` and related headers (`public/_headers`) also block runtime external transmission at the browser level
- No analytics, logging, or error-reporting SDKs; no external fonts or CDNs
- Production builds never write plaintext, keys, or QR contents to the console
- Plaintext persistence is forbidden (in both localStorage and IndexedDB). An auto-clear timer runs when the app is left in the background

## 3. Out of scope (explicit non-goals — full text shown in the in-app "About security" settings section)

1. Compromise of the OS, browser, or firmware
2. Keyloggers, screen recording, screenshots
3. Malware that captures camera frames
4. Supply-chain compromise at initial PWA installation or reinstallation
5. Physical theft of the device
6. The user themselves accidentally sharing a secret QR
7. Loss of keys through browser data deletion

The "offline" indicator is auxiliary information about network state; it is never treated or presented as proof of safety.
The explanatory screen and acknowledgement checkbox shown when the device returns to offline do not verify or restore the safety of the device.

## 4. Attacker model and countermeasures

> **Status note (v2):** rows and trade-offs below that reference RSA (the T1
> mitigation wording, T4, and the §5 recommendation of the RSA scheme) describe
> the retired v1 RSA-era design and are retained as historical record. The
> current implementation is PQ-only — ML-KEM-1024 envelopes with optional
> ML-DSA-87 signatures — and the RSA path has been removed entirely (schema
> validation rejects non-`A256GCM` OCM1 messages; see §7 and the README's
> "Breaking changes" section). "No signatures (v1)" no longer applies to v2
> signed envelopes.

| #   | Threat                              | Vector                                                          | Countermeasures                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Residual risk                                                                                                    |
| --- | ----------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| T1  | Interception of a ciphertext QR     | Photographing / relaying the QR                                 | AES-256-GCM / RSA-OAEP-3072 hybrid. Cannot be decrypted without the key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Decryptable if the key leaks                                                                                     |
| T2  | Ciphertext tampering / substitution | QR replacement                                                  | GCM authentication tag + AAD verification (binding v/type/alg/keyId/createdAt). On failure, nothing is displayed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Substitution with a different legitimate ciphertext is undetectable (no signatures; a v1 non-goal)               |
| T3  | Exposure of a symmetric-key QR      | Photographing, screen sharing, cloud sync, clipboard sync       | Sensitive-information warning on display, strong confirmation (checkbox required) before PNG/SVG/copy, sync-warning toast on copy, no key material in file names                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Optical duplication at the moment of display cannot be prevented (non-goal 6)                                    |
| T4  | Extraction of the RSA private key   | XSS, browser extensions, shared devices                         | non-extractable CryptoKey (`exportKey`/`wrapKey` impossible), CSP script-src 'self', zero external scripts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | OS/browser compromise (non-goal 1)                                                                               |
| T5  | Scanning a malicious QR             | Spoofed QR                                                      | Prefix + CBOR + Zod strict validation, fixed byte-length validation, consistency check against the pre-selected type, no automatic decryption (explicit user action required), explicit rejection of out-of-scope payloads                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —                                                                                                                |
| T6  | Public-key substitution (MitM)      | Fake public-key QR                                              | Fingerprint (SHA-256 → 4×4 digits) shown on the import confirmation screen, with wording prompting in-person comparison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | If the user skips the comparison                                                                                 |
| T7  | Supply chain (build/distribution)   | Dependency tampering, Pages tampering                           | Committed aube-lock.yaml, CI runs `aube ci` (frozen lockfile), registry advisory checks and a provenance no-downgrade policy, least-privilege API token, no update mechanism (permanent offline operation)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Compromise at initial installation or reinstallation is non-goal 4                                               |
| T8  | Plaintext lingering in the browser  | Tab left open, device lending                                   | Auto-clear after encryption ON by default, background auto-clear also ON by default with a fixed delay, "Clear all plaintext" action, no restore on reload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Memory forensics (non-goals 1/5)                                                                                 |
| T9  | GCM failure via IV reuse            | Implementation bug                                              | 12-byte CSPRNG IV per encryption. Reuse-detection tests, fresh generation per key. No counter scheme — no state is kept                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2^-96 collision (negligible)                                                                                     |
| T10 | Downgrade / confusion attacks       | Rewriting alg/type                                              | algorithm/type/v sealed into the AAD (rewriting fails tag verification); unknown versions/schemes rejected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —                                                                                                                |
| T11 | Information leakage via storage     | localStorage misuse, IndexedDB residue, export destinations     | App-managed IndexedDB/localStorage never persist message ciphertexts or key QRs; there is no saved-QR feature and no `qrArtifacts` store at all. The only permitted localStorage keys are `{oc-theme, oc-lang, oc-offline-ack-pending}` (checked by automated tests). `oc-lang` holds only the UI language (`en`/`ja`); the acknowledgement marker's value is fixed to `"1"` — a non-sensitive pending state containing no keys, plaintext, or ciphertext. All keys are deleted on `oc-*` initialization (the UI language therefore reverts to the English default); only the `online-detected` wipe re-sets the acknowledgement-pending marker after deletion and before the `wiped` publish, and `user-requested` does not re-set it. Verified by plaintext-non-persistence tests and store-absence tests. User-explicit PNG/SVG/ZIP downloads and the clipboard may remain in the OS, browser, or sync destinations and are outside the scope of wipe | Device theft + profile extraction (non-goal 5). Residue at explicit export destinations is outside the wipe scope |
| T12 | Unauthorized camera use             | Always-on camera                                                | getUserMedia only while the scan modal is shown, tracks stopped on close, Permissions-Policy camera=(self)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Camera malware (non-goal 3)                                                                                      |

As a cross-cutting countermeasure for T8/T11, OnlineGate blocks all functionality while online, and the offline→online transition fires TransientClear, which immediately clears plaintext, decryption results, and result payloads. A displayed-online recommit restarts boot's sentinel probe at most once, but the displayed edge itself is never grounds for, nor a direct trigger of, a wipe. While the display probe is in a false-negative state, there is residual risk that InstallScreen does not block. The offline indicator itself is never treated as proof of safety.

## 5. Design trade-offs (explicit)

- **The symmetric key is extractable**: generating the symmetric-key QR is a spec requirement. The exposure surface is reduced by T3's UI countermeasures. For counterparts needing stronger protection, the RSA scheme is recommended (its private key is non-extractable).
- **No signatures or sender authentication (v1)**: authenticity of a ciphertext (who created it) is not guaranteed. Introducing signatures into the envelope would be a v2 change.
- **createdAt is metadata**: there is no replay-protection mechanism (for offline use this is left to the recipient's contextual judgment). AAD sealing only makes tampering detectable.
- **Private-key backup (OCB1) not implemented**: flag permanently OFF. Any implementation must satisfy these conditions: encryption under a passphrase-derived key, a random salt/IV, and a restore test.
- **No update path**: eliminating the update path is a maintainer decision on operational policy. Installed devices operate permanently offline; a new version is installed fresh only after the device has been sanitized. A simple full format alone does not guarantee erasure on flash/SSD media — use a media-appropriate sanitization procedure (e.g. NIST SP 800-88) or destroy the media when assurance matters.

## 5.1 Supply-chain incidents actually detected and handled during initial build-out (2026-07-21)

| Incident                                                                                                                                                                                       | Assessment                                                | Action                                                                                                    |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| Malware advisory on some versions of `react-hook-form` (MAL-2026-2853: 7.73.0)                                                                                                                  | Bare range rejected based on the registry advisory        | Pinned to safe version `7.82.0`                                                                           |
| Compromised versions of `eslint-config-prettier` (MAL-2025-6022: 8.10.1/9.1.1/10.1.6/10.1.7)                                                                                                    | Same as above                                             | Pinned to clean version `10.1.8`                                                                          |
| `workbox-build@7.4.1` switched its default `@surma/rollup-plugin-off-main-thread` to a personal fork `@trickfilm400/...@3.0.0-pre1` (matches upstream GitHub v7.4.1 = intentional upstream change) | Judged a reduction of the trust surface (trust downgrade) | Aliased to `@surma/...@2.2.3` via `aube.overrides` and verified build behavior                            |
| Only `radix-ui@1.6.4` (the umbrella package) lacked provenance (the adjacent 1.6.3 / 1.7.0-rc are attested)                                                                                     | Not adopted due to a possible supply-chain incident       | Removed the umbrella package; using the provenance-preserving scoped `@radix-ui/react-*` packages         |
| `cbor-extract` install script                                                                                                                                                                   | Unreviewed build scripts are not executed (aube default)  | Operating on the pure-JS path without native acceleration (browsers use the JS path anyway)               |

### Current re-check (2026-07-25)

| Target | Status | Notes |
| --- | --- | --- |
| `@noble/post-quantum` exact pin `0.6.1` | Latest; no advisories (repo / GHSA / OSV clean) | Not independently audited (self-audit only). Pin kept |
| `react-hook-form@7.82.0` / `eslint-config-prettier@10.1.8` | Re-checked clean | Supply-chain pins maintained |
| `sharp@0.35.2` via `wrangler@4.114.0` → `miniflare@4.20260722.0` (exact pin) | **RESOLVED** `GHSA-f88m-g3jw-g9cj` (CVE-2026-33327 / CVE-2026-33328 / CVE-2026-35590 / CVE-2026-35591, published 2026-07-17); affected versions below 0.35.0 | Current lock contains the fixed version |
| `react-router@8.3.0` (exact; replaced the 7.x-only `react-router-dom` wrapper) | **RESOLVED** `GHSA-qwww-vcr4-c8h2` (published 2026-07-24, high): RSC Mode CSRF bypass; vulnerable `>=7.12.0 <8.3.0` | The vulnerable path (RSC mode with server-executed actions) was never used by this client-only PWA; upgraded to the fixed 8.3.0 regardless |
| `brace-expansion` forced to `5.0.8` via `aube.overrides` (both major lines) | **RESOLVED** `GHSA-mh99-v99m-4gvg` (high): DoS via unbounded expansion; vulnerable `<=5.0.7`, no fixed 2.x release | Dev-chain only (`workbox-build` glob tooling; repo-controlled inputs). Build and full test suite re-verified after the override. `aube audit` exits 0 |
| Routine in-range updates | Applied | `react@19.2.8`, radix-family minors, `wrangler@4.114.0` |
| Toolchain | `node@26.5.0` / `aube@1.32.0` pinned | `mise.toml` |
| FIPS 203 / 204 errata | Prospective correction only | NIST FIPS 204 sheet updated 2026-02-27. No API / size impact |

## 6. Mapping to verification

- Zero external transmission: e2e tests confirm that every in-page network request has origin self only, plus CSP headers
- Plaintext non-persistence / cleanliness of the localStorage allowlist `{oc-theme, oc-lang, oc-offline-ack-pending}` and the pending value `"1"`: integration and e2e tests
- Decryption failure on AAD mismatch, tampering, and wrong keys: unit tests
- IV uniqueness and length: unit tests
- Strong-confirmation and warning UI: UI tests

## 7. v2 (post-quantum) addendum — 2026-07-22

Contract: this document together with `docs/qr-protocol-v2.md` / `docs/boot-and-reset-v2.md` / `docs/security-review.md`.

| ID  | Asset / threat                                | Countermeasures                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Residual risk (explicit)                                                                                                                                                                                                                              |
| --- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T13 | Theft of PQ seeds (KEM 64B / DSA 32B)         | Only seeds are stored (expanded keys never persisted), AES-GCM encrypted under a non-extractable Vault key, AAD binds identityId/role/algorithm/keyId/publicKeySha256, and after decryption a seed is not used until public-key regeneration matches                                                                                                                                                                                                                                  | Malicious code that runs first on the same origin can use the Vault key (a known Web Crypto boundary)                                                                                                                                                  |
| T14 | Harvest-now-decrypt-later attacks             | The active policy is maximum: ML-KEM-1024+HKDF-SHA256+AES-256-GCM (default `MLKEM1024_A256GCM`). balanced (the ML-KEM-768 family) on the wire is `UNSUPPORTED_ALGORITHM` at the boot/operational boundary. RSA/ECDH fully removed                                                                                                                                                                                                                                                    | noble is not independently audited; JS side-channel resistance is not guaranteed (security-review.md)                                                                                                                                                  |
| T15 | Sender impersonation                          | Sign-then-Encrypt (ML-DSA-87, fixed context; suite `ML-KEM-1024+ML-DSA-87+HKDF-SHA256+A256GCM`), body not displayed until signature verification succeeds, signing-key ID kept inside the encryption. ML-DSA-65 is reserved for balanced only and is never used in active cryptographic operations                                                                                                                                                                                    | A signature only means "valid against a stored public key". Mapping to a person requires out-of-band fingerprint comparison (distinguished by trust state)                                                                                             |
| T16 | Mixing / tampering in multi-QR transfers      | transferId separation, FRAME_MISMATCH, full SHA-256/length/type verification on completion, no cryptographic processing starts before completion                                                                                                                                                                                                                                                                                                                                    | SHA-256 provides transfer integrity, not sender authentication                                                                                                                                                                                        |
| T17 | Residual data on online exposure              | wipe-on-online (ON by default; fires only when network-confirmed; the boot state machine runs ahead of the Router)                                                                                                                                                                                                                                                                                                                                                                  | Limited to "reducing residual data when the current code was able to run after connectivity". Cannot defend against same-origin malicious code, physical recovery, or code that ran before the update. Physical erasure is not guaranteed (best-effort logical deletion + Vault key shredding only) |
| T18 | Erroneous wipe due to reachability misdetection | Separation of the display probe (navigator.onLine + manifest probe) from the destructive probe (sentinel body match), nonce + no-store, generation number + AbortSignal only until sentinel confirmation, token/wipe decision latch after confirmation, at most once per transition, no firing on the install path (no sensitive data present), maintenance token. A displayed offline nudges boot non-destructively; a displayed-online recommit restarts the normal sentinel probe at most once | If a captive portal passes the sentinel body through unchanged, that is treated as equivalent to "actually reachable". Features may open while the display probe is in a false-negative state                                                          |

- **Destructiveness of RSA removal**: after updating to v2, existing OCM1-RSA ciphertexts are unrecoverable (the non-extractable RSA private key cannot be rescued). The update UI and the README state this explicitly, together with the legacy-item count and a deletion path (no decrypt-only compatibility is retained).
- Still maintained in v2: plaintext non-persistence, zero external transmission, and a localStorage allowlist of `{oc-theme, oc-lang, oc-offline-ack-pending}` only. `oc-lang` holds only the UI language (`en`/`ja`); the acknowledgement marker is a non-sensitive pending state with the fixed value `"1"`. All keys are deleted on `oc-*` initialization; only the `online-detected` wipe re-sets the acknowledgement-pending marker after deletion and before the `wiped` publish, and `user-requested` does not re-set it.
